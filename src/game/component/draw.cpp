#include "draw.h"

namespace dte {
    void DrawComponent::update(Entity &entity,
        SDL_Renderer *renderer,
        Uint32 totalTimeMs,
        float remainderFrames) {}
    DrawComponent::~DrawComponent() {}
}